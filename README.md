CustomExternalLookup
====================

SharePoint 2010 Custom Field Type. The field allows to set SQL SELECT query as a datasource and use that data in HTML select and SharePoint EntityPicker controls
